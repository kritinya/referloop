/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // Note: if you are using next/image, you might also need to configure unoptimized: true here for static exports to work.
};

export default nextConfig;